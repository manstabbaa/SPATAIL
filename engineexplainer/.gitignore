# Secrets — never commit
.env
.env.local
*.key
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / web build artifacts (when we add a build step)
node_modules/
dist/
build/

# Generated outputs that can be regenerated by the authoring pipeline
# (keep the GLB committed if it's the canonical asset; uncomment to ignore)
# engine/v8_engine.glb
