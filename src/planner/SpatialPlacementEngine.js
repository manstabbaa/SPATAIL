// SpatialPlacementEngine — public entry.
// See pipeline/spatail/placement_engine.js for the per-mode strategies.
export {
  createLayoutState,
  placeElement,
  ROOM_DIMENSIONS,
} from "../../pipeline/spatail/placement_engine.js";
