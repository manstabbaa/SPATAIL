# Build artifacts — regenerated by `python studio/run.py` / educator.py
out/

# Educator-generated briefs/scenes are derived per question and need not be
# tracked. Keep only the canonical hand-authored scenes (newtons_laws.json).
brief/*.json
scenes/*.json
!brief/newtons_laws.json
!scenes/newtons_laws.json
