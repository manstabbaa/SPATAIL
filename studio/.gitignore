# Build artifacts — regenerated by `python studio/run.py` / educator.py
out/

# Educator-generated briefs/scenes (the canonical newtons_laws.json are kept;
# per-question slugged files are derived and need not be tracked)
brief/how_*.json
scenes/how_*.json
