"""studio/director — the agent that designs SPATAIL experiences by composing mechanics.

director.compose(understanding, assets) -> beats (LLM agent via llm.py, or deterministic).
experience.build_modular_experience(...) -> the full platform-agnostic modular contract
(understanding + stage + assets + beats-of-mechanics) that every runtime renders.
"""
